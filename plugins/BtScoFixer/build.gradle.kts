version = "1.0.0"
description = "Auto-detects Bluetooth SCO in Discord calls and optionally routes audio via A2DP instead"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial release
        """.trimIndent(),
    )

    deploy.set(false)
}
