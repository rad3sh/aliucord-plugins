version = "1.0.0" // Plugin version. Increment this to trigger an update
description = "Adds a button to lock the screen to landscape when watching a stream in a voice call" // Plugin description that will be shown to user

aliucord {
    // Changelog of your plugin
    changelog.set(
        """
        # 1.0.0
        * Initial plugin release!
        """.trimIndent(),
    )

    deploy.set(true)
}
